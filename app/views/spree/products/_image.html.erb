 <% if defined?(image) && image %>
  <%= image_tag image.attachment.url(:product), :itemprop => "image", :id => 'zoom', :data => {'zoom-image' => image.attachment.url(:large)} %>
<% else %>
  <%= product_image(@product, :itemprop => "image", :id => 'zoom') %>
<% end %>
