<ul style="margin-top: 40px;">
  <% Spree::Taxonomy.includes(root: :children).each do |taxonomy| %>
    <%# cache taxonomy do %>
      <li class="head" ><%= Spree.t(:shop_by_taxonomy, :taxonomy => taxonomy.name) %></li>
      <%= taxons_tree(taxonomy.root, @taxon, Spree::Config[:max_level_in_taxons_menu] || 1) %>
    <%# end %>
  <% end %>
</ul>
